pacman -S sox pulseaudio ffmpeg libmad man-db man-pages git github-cli vim  \
    neovim fzf bat hexedit gcc gdb binutils patchelf make python nodejs npm \
    go rust lua curl fish starship htop python-pip python-pipx pkgfile eza  \
    neofetch pacman-contrib figlet ripgrep

gh extension install dlvhdr/gh-dash

# language servers
pacman -S clang     # for clangd
pacman -S lua-language-server
cargo install taplo-cli --features lsp     # .toml
npm install -g typescript-language-server
pipx install pyright

# setup ssh
# setup gpg
# git config user.name and user.email


# dotfiles/configs
ln -s  ~/vytdev/.bashrc  ~/.bashrc
ln -s  ~/vytdev/.vimrc   ~/.vimrc
ln -s  ~/vytdev/nvim     ~/.config/nvim
